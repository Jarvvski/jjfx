[package]
name = "wsg"
version = "0.8.1"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
anyhow = "1"
jiff = "0.2"
wsg-core = { path = "../wsg-core" }

[dev-dependencies]
tempfile = "3"
wsg-core = { path = "../wsg-core" }

[lints.rust]
unsafe_code = "forbid"
unused = { level = "warn", priority = -1 }

[lints.clippy]
all = { level = "warn", priority = -1 }
